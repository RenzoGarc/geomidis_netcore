@model IEnumerable<GeomidisSystem.Models.PaisExample>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CANBEN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CANBEN2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CANBEN3)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CANBEN4)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CANBEN5)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CANBEN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CANBEN2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CANBEN3)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CANBEN4)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CANBEN5)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
